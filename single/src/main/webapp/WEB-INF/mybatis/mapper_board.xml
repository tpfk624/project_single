<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.kitri.single.board.dao.BoardDao">
	
	<insert id = "writeArticle" parameterType="boardDto">
		insert into single_board(board_num, board_list_num, user_id, user_nickname, board_subject,
		board_content, board_views, board_createdate, board_updatedate, board_deletedate, board_statecode)
		values(board_num_seq.nextval, #{boardListNum}, #{userId}, #{userNickname},
		 #{boardSubject} , #{boardContent}, '0', sysdate, null, null, #{boardViews})
	</insert>
	
</mapper>
   
   