<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.kitri.single.board.dao.ReplyDao">
	
	<insert id = "writeArticle" parameterType="replyDto">
		<!-- insert all
		into single_board
		values(board_num_seq.nextval, #{boardListNum}, #{userId}, #{userNickname},
		 #{boardSubject} , #{boardContent}, '0', sysdate, null,null, '1')
		into single_reply (reply_num, board_num, user_id, user_nickname, reply_content, reply_createdate)
      	values (reply_num_seq.nextval, #{boardSubject}, #{ref}, 0, 0, 0, 0)
    	select * from dual -->
	</insert>
	
</mapper>